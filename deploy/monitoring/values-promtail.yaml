# Promtail values — ship node/container logs to the single-binary Loki service.
config:
  clients:
    # gateway is disabled, push straight to the loki service
    - url: http://loki:3100/loki/api/v1/push

resources:
  requests:
    cpu: 20m
    memory: 64Mi
  limits:
    memory: 128Mi
